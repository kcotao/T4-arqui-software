apiVersion: v1
kind: Service
metadata:
  name: threads-service
spec:
  type: LoadBalancer # 👈 Esto crea la URL pública
  ports:
    - port: 80 # Puerto público
      targetPort: 8000 # Puerto de tu app
  selector:
    app: threads
